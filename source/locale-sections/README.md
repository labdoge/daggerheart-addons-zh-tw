# Locale Sections

Each JSON file is a merge layer for `lang/zh-TW.json`.

The numbered add-on files are intentionally empty at initialization. Add translations only after auditing the add-on's real i18n namespace, manifest language files, settings, templates, and runtime UI.
